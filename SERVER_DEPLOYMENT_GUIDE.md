# دليل التثبيت والتشغيل على خادم Windows Server (بدون Docker)

تم إعداد هذا الدليل لتشغيل نظام المكتبة الإلكترونية مباشرة كـ **خدمة ويندوز أصلية (Windows Service)** على خادم **Windows Server** بسرعة وخفة واستقرار تام ودون الحاجة لأي محرك حاويات أو Docker.

---

## متطلبات التشغيل

1. **PHP 8.2+** مثبت على السيرفر (مع تفعيل الإضافات: `pdo_sqlite`, `sqlite3`, `mbstring`, `fileinfo`, `openssl`).
2. **Composer** (اختياري إذا كانت ملفات الـ vendor منسوخة بالفعل).
3. صلاحيات مسؤول (Administrator) على السيرفر.

---

## طريقة التثبيت والتشغيل السريع (كخدمة ويندوز رسمية) 🚀

تعتمد هذه الطريقة على تشغيل تطبيق Laravel في الخلفية مع الإقلاع التلقائي عند تشغيل السيرفر.

### الخطوات:

1. افتح موجه أوامر **PowerShell كمسؤول (Run as Administrator)**.
2. انتقل إلى مجلد المشروع على السيرفر:
   ```powershell
   cd "C:\Users\Al_Hamed\Desktop\elibrary"
   ```
3. نفذ سكربت التثبيت التلقائي المرفق:
   ```powershell
   .\install-windows-service.ps1
   ```
   *(إذا كان PHP مثبتاً في مسار مخصص، حدد المسار عبر: `.\install-windows-service.ps1 -PhpPath "C:\php\php.exe"`)*

4. **ماذا يفعل السكربت تلقائياً؟**
   - ينشئ خدمة ويندوز باسم `ELibraryService`.
   - يربطها بتطبيق Laravel وتشغيله على المنفذ `80`.
   - يجعل الخدمة تقلع تلقائياً مع بدء تشغيل السيرفر (`Automatic Startup`).

---

## إدارة الخدمة ولوحة التحكم (ELibraryAdmin.exe) 🎛️

المشروع مزود بأداة تحكم تنفيذية رسومية: **`ELibraryAdmin.exe`**
- افتح البرنامج من مجلد المشروع.
- يمكنك عبره بضغطة زر:
  - **تشغيل / إيقاف** الخدمة بسهولة.
  - **إعادة تشغيل** خادم المكتبة عند تعديل أي ملفات.
  - معرفة حالة الخادم ورابط الدخول المباشر.

---

## ضبط جدار الحماية (Firewall) للسماح بالدخول عبر الشبكة 🌐

لكي يتمكن جميع الموظفين والمستخدمين في الشبكة المحلية من فتح المكتبة، افتح المنفذ 80 بتنفيذ هذا الأمر في PowerShell كمسؤول:

```powershell
New-NetFirewallRule -DisplayName "ELibrary Web Port 80" -Direction Inbound -LocalPort 80 -Protocol TCP -Action Allow
```

ثم يمكنهم الدخول مباشرة بكتابة عنوان السيرفر في المتصفح، مثلاً:
```text
http://192.168.1.100
```
أو عبر اسم الخادم في الشبكة.
